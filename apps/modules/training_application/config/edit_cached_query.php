<?php if (!defined('BASEPATH')) exit('No direct script access allowed');
$config["edit_cached_query"] = 'SELECT `ww_training_application`.`application_id` as record_id, `ww_training_application`.`created_on` as "training_application.created_on", `ww_training_application`.`created_by` as "training_application.created_by", `ww_training_application`.`modified_on` as "training_application.modified_on", `ww_training_application`.`modified_by` as "training_application.modified_by", ww_training_application.remarks as "training_application.remarks", ww_training_application.total_hours as "training_application.total_hours", DATE_FORMAT(ww_training_application.date_to, \'%M %d, %Y\') as "training_application.date_to", DATE_FORMAT(ww_training_application.date_from, \'%M %d, %Y\') as "training_application.date_from", ww_training_application.estimated_cost as "training_application.estimated_cost", ww_training_application.venue as "training_application.venue", ww_training_application.source_id as "training_application.source_id", ww_training_application.course_id as "training_application.course_id", ww_training_application.competency_id as "training_application.competency_id", ww_training_application.type_id as "training_application.type_id"
FROM (`ww_training_application`)
WHERE `ww_training_application`.`application_id` = "{$record_id}"';