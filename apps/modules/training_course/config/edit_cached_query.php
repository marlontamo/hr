<?php if (!defined('BASEPATH')) exit('No direct script access allowed');
$config["edit_cached_query"] = 'SELECT `ww_training_course`.`course_id` as record_id, `ww_training_course`.`created_on` as "training_course.created_on", `ww_training_course`.`created_by` as "training_course.created_by", `ww_training_course`.`modified_on` as "training_course.modified_on", `ww_training_course`.`modified_by` as "training_course.modified_by", ww_training_course.position_id as "training_course.position_id", ww_training_course.planned as "training_course.planned", ww_training_course.facilitator as "training_course.facilitator", ww_training_course.provider_id as "training_course.provider_id", ww_training_course.type_id as "training_course.type_id", ww_training_course.category_id as "training_course.category_id", ww_training_course.course as "training_course.course", ww_training_course.remarks as "training_course.remarks", ww_training_course.length_of_service as "training_course.length_of_service", ww_training_course.cost as "training_course.cost", ww_training_course.with_bond as "training_course.with_bond"
FROM (`ww_training_course`)
WHERE `ww_training_course`.`course_id` = "{$record_id}"';